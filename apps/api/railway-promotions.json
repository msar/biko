{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm exec --workspace=@biko/api -- prisma generate"
  },
  "deploy": {
    "startCommand": "npm run sync:promotions --workspace @biko/api",
    "cronSchedule": "0 9 * * *",
    "restartPolicyType": "NEVER"
  }
}
