{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm exec --workspace=@biko/api -- prisma generate"
  },
  "deploy": {
    "startCommand": "npm run railway:release --workspace @biko/api && npm run start --workspace @biko/api",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
