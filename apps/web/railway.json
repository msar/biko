{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build --workspace @biko/web"
  },
  "deploy": {
    "startCommand": "npm run start --workspace @biko/web",
    "restartPolicyType": "ON_FAILURE"
  }
}
